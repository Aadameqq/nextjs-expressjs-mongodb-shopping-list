import React from "react";

import styles from "../styles/header.module.css";

const Header: React.FC = () => {
  return <div className={styles.header}>Shopping List</div>;
};

export default Header;
